
  # QuestList

  This is a code bundle for QuestList. The original project is available at https://www.figma.com/design/eiC68imY2n3HW7XliFbFCD/QuestList.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  